export class ScaleBar {
    public month: string;
    public scaleBar: number;

    constructor(month: string, scaleBar: number){
        this.month = month;
        this.scaleBar = scaleBar;
      }
}